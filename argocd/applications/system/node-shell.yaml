apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-shell
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: voidnode
    app.kubernetes.io/component: system
  annotations:
    owner: platform
    repo: khaddict/voidnode
spec:
  project: default
  source:
    repoURL: https://github.com/khaddict/voidnode.git
    targetRevision: main
    path: argocd/apps/node-shell
    helm:
      releaseName: node-shell
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: node-shell
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
