global
    log /dev/log local0
    log /dev/log local1 notice
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin
    stats timeout 30s
    user haproxy
    group haproxy
    daemon

    # Default SSL material locations
    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private

    # See: https://ssl-config.mozilla.org/#server=haproxy&server-version=2.0.3&config=intermediate
    ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
    ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
    ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
    log global
    mode http
    option httplog
    option dontlognull
    option forwardfor
    timeout connect 5s
    timeout client 50s
    timeout server 50s
    timeout tunnel 1h
    timeout client-fin 1m

    errorfile 400 /etc/haproxy/errors/400.http
    errorfile 403 /etc/haproxy/errors/403.http
    errorfile 408 /etc/haproxy/errors/408.http
    errorfile 500 /etc/haproxy/errors/500.http
    errorfile 502 /etc/haproxy/errors/502.http
    errorfile 503 /etc/haproxy/errors/503.http
    errorfile 504 /etc/haproxy/errors/504.http

frontend web_frontend
    bind :80
    bind :443 ssl crt {{ public_domain }}.bundle.pem accept-proxy

    # Force HTTPS
    http-request redirect scheme https unless { ssl_fc }

    # Forward useful headers
    http-request set-header X-Forwarded-Proto https if { ssl_fc }
    http-request set-header X-Forwarded-Port 443 if { ssl_fc }
    http-request set-header X-Forwarded-Port 80 unless { ssl_fc }

    # Hosts
    acl host_web       hdr(host) -i {{ public_domain }} www.{{ public_domain }}
    acl host_website   hdr(host) -i website.{{ public_domain }}
    acl host_homepage  hdr(host) -i homepage.{{ public_domain }}
    acl host_images    hdr(host) -i images.{{ public_domain }}
    acl host_matomo    hdr(host) -i matomo.{{ public_domain }}

    # Routing
    use_backend backend_web if host_web
    use_backend backend_website if host_website
    use_backend backend_homepage if host_homepage
    use_backend backend_images if host_images
    use_backend backend_matomo if host_matomo

    # Default backend
    default_backend backend_web

backend backend_web
    http-request set-header Host www.{{ domain }}
    server web www.{{ domain }}:443 ssl verify none sni str(www.{{ domain }}) check

backend backend_website
    http-request set-header Host website.{{ domain }}
    server website website.{{ domain }}:443 ssl verify none sni str(website.{{ domain }}) check

backend backend_homepage
    http-request set-header Host homepage.{{ domain }}
    server homepage homepage.{{ domain }}:443 ssl verify none sni str(homepage.{{ domain }}) check

backend backend_images
    http-request set-header Host images.{{ domain }}
    server images images.{{ domain }}:443 ssl verify none sni str(images.{{ domain }}) check

backend backend_matomo
    http-request set-header X-Forwarded-Host matomo.{{ public_domain }}
    http-request set-header Host matomo.{{ domain }}
    server matomo matomo.{{ domain }}:80 check
