name: generate_certificate_server
pack: st2_voidnode
runner_type: orquesta
description: Generate a server certificate, copy it to the VM, and store it in Vault.
entry_point: workflows/generate_certificate_server.yaml
enabled: true

parameters:
  vm_name:
    type: string
    required: true
    description: The VM name defined in main.yaml.
